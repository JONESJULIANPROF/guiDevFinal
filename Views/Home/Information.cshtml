@model IEnumerable<FatTrack>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
   

}
<h1>Your Info</h1>
<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Starting Weight</th>
            @*<th>Current Weight</th>*@
            <th>Desired Weight</th>
            <th>Height</th>
            <th>Gender</th>
            <th>Activity Level</th>
            <th>Birthday</th>
            <th>Age</th>
            <th>BMR</th>
            <th>Calories Needed</th>
        </tr>

    </thead>

    <tbody>
        @foreach (var mod in Model)
        {

            var Today = DateTime.Today;
            //Today.ToString()

            var Birthday = mod.Birthday;
            Int32 age;
            age = Today.Year - Birthday.Year;

            mod.Age = age;

            if (mod.Gender == "M")
            {
                double bmr = Double.Parse(mod.Bmr);
                bmr = 66 + (6.3 * mod.CurrentW) + (12.9 * mod.HeightInches) - (6.8 * mod.Age);
            }
            else if (@mod.Gender == "F")
            {
                mod.Bmr = 655 + (4.3 * mod.CurrentW) + (4.7 * mod.HeightInches) - (4.7 * mod.Age);
            }
            else
            {
                mod.Bmr = 0;
            }












            <tr>
                <td>@mod.FirstN</td>
                <td>@mod.Email</td>
                <td>@mod.StartingW</td>
                
                <td>@mod.DesiredW</td>
                <td>@mod.HeightInches</td>
                <td>@mod.Gender</td>
                <td>@mod.ActivityLevel</td>
                <td>@mod.Birthday</td>
                <td>@mod.Age</td>
                @* BMR *@

                @* Calories Needed *@
                


            </tr>

        }

        }





    </tbody>

</table>

